---
---

docs: sync root README toolchain versions with mise.toml.

Intentionally empty changeset (no version bump). The root README is not
part of the published package (`files: ["dist/**"]`), so correcting its
toolchain prose has no effect on any released package.
